/** @type {import('next').NextConfig} */
const nextConfig = {
  // Explicitly tell Next.js where the src directory is
  // and ensure the @/ alias resolves correctly
  experimental: {},
}

module.exports = nextConfig
